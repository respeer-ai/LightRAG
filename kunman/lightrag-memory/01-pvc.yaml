apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lightrag-${LINERA_TAG}-pvc
  namespace: kube-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
