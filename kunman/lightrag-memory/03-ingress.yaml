apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lightrag-${LINERA_TAG}-ingress
  namespace: kube-system
  annotations:
    k8s.kuboard.cn/displayName: lightrag-${LINERA_TAG}-service
    k8s.kuboard.cn/workload: lightrag-${LINERA_TAG}-service
    kubernetes.io/ingress.class: "traefik-internet"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure, web
    traefik.ingress.kubernetes.io/router.middlewares:
      kube-system-http-redirect-https@kubernetescrd,
      kube-system-http-redirect-https-testing@kubernetescrd,
      kube-system-common-headers@kubernetescrd

spec:
  tls:
    - hosts:
        - '*.linera-rag.respeer.ai'
        - linera-rag.respeer.ai
      secretName: linera-rag-respeer-ai-tls
  rules:
    - host: lightrag-${LINERA_TAG}.linera-rag.respeer.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lightrag-${LINERA_TAG}-service
                port:
                  number: 9621
